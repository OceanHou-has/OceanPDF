### 安装依赖

#### 前端
```bash
cd frontend
npm install --registry=https://registry.npmmirror.com/
npm run dev
```

#### 后端
```bash
cd backend
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

```
#### PDS服务
```bash
cd dps
$env:DOC_LAYOUT_DEVICE="gpu:0";
$env:OCR_USE_DOC_UNWARPING="0";
$env:OCR_USE_DOC_ORIENTATION_CLASSIFY="0";
$env:OCR_USE_TEXTLINE_ORIENTATION="0";
.\venv\Scripts\python .\pp_doclayoutv2_api_new.py --host 127.0.0.1 --port 8001
```
cpu版本启动
conda activate paddle-ocr
$env:DOC_LAYOUT_MODEL_NAME = "PP-DocLayout_plus-L"
python .\pp_doclayoutv2_api_new.py --host 127.0.0.1 --port 8001

### 运行项目

#### 开发环境

1. 启动主后端服务
```bash
cd backend
.\.venv\Scripts\Activate.ps1
python -m uvicorn app.main:app --reload --port 8000
```


3. 启动前端应用
```bash
cd frontend
npm run dev
```